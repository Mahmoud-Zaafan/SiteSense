# CV Inference Service - Requirements
# Versions pinned to prevent pip resolver backtracking (was re-downloading
# multi-GB torch wheels while searching for a compatible combo).

# --- Torch stack (install from PyTorch CU121 index, see Dockerfile) ---
torch==2.2.2
torchvision==0.17.2

# --- Core detection + tracking ---
rfdetr==1.3.0
ultralytics==8.4.37
boxmot>=11.0.0,<12.0.0
opencv-python-headless==4.10.0.84

# --- DINOv3 Re-ID (persistent identity matching) ---
transformers==4.56.2
accelerate==0.34.2
huggingface_hub==0.34.4

# --- Kafka producer ---
confluent-kafka==2.5.3

# --- Utilities ---
numpy==1.26.4
pyyaml==6.0.2
Pillow==10.4.0
